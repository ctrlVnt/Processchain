VERSIONE FULL
- per transazione regalo(senza l'attesa non attiva) a nodi amici -- proposta di gestire la TP
in termini di SO_BLOCK_SIZE (al posto di SO_BLOCK_SIZE - 1),dove l'ultima la inviamo ad un nodo amico
- nodi amici vengono scelti da parte degli UTENTI, in seguito dello scattare di SO_HOPS,
quindi l'utente sceglie casualmente un amico del pool di amici del nodo scelto precedentemente